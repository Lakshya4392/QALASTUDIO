#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running pre-commit checks..."

# Run lint-staged
npx lint-staged

if [ $? -ne 0 ]; then
  echo "❌ Lint checks failed. Fix errors before committing."
  exit 1
fi

echo "✅ Pre-commit checks passed!"
